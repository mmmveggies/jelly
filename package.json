{
  "name": "jelly-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "parcel src/index.html",
    "bundle": "npm run clean && npm run build",
    "build": "parcel build src/index.html -d docs --public-url ./",
    "clean": "rimraf docs dist .cache",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^14.10.1",
    "parcel": "^1.12.4",
    "rimraf": "^3.0.2",
    "typescript": "^4.0.2"
  }
}